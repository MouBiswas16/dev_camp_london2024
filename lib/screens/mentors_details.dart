import 'package:flutter/material.dart';

class MentorsDetails extends StatelessWidget {
  const MentorsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blue,
      ),
    );
  }
}
